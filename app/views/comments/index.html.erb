<h4>Click on any Framework to see it's comments</h4>
<ul class="list-unstyled">
  <% @frameworks.each do |framework| %>
    <li><h4><%= link_to framework.name, "/frameworks/#{framework.id}" %></h4></li>
  <% end %>
</ul>
